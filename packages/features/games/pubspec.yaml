name: games
publish_to: 'none'
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.14.10
  shared_ui:
    path: ../../shared_ui

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/